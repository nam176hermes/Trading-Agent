# NautilusTrader 1.227.0 to 1.231.0 release, API, build, and semantic impact

## Decision

**P1-U01 source-mapping result: PASS.** The exact 1.231.0 candidate remains suitable for later isolated qualification. No direct API removal, unexplained callback change, or ambiguous accounting change remains in this source-delta packet. This is not build, runtime, promotion, paper-readiness, or production evidence. The candidate stays `CANDIDATE_CONTEXT_ONLY` until the later gates complete.

The closed machine-readable authority is
[`direct-api-contract.json`](direct-api-contract.json). Its closed v2 schema contains 33 current/planned API surfaces, 40 highlighted release-delta records, an exact 42-site import inventory, and a frozen 153-record invoked-member map. Its per-bullet manifest classifies all 992 bullets in the fixed 1.228.0 through 1.231.0 `RELEASES.md` range. Every direct or indirect behavior change is routed to an executable P1-U05, P1-U06, or P1-U07 scenario. There are zero `BLOCK` and zero unknown classifications.

## Exact evidence boundary

| Role | Version and Git object | Tree | Meaning |
| --- | --- | --- | --- |
| rollback | `v1.227.0` / `280ae1762df51a492a4ce71506a40b5c8706def5` | `8dbb8eed0c875310ffb8f576cb07d7cd40da1556` | unchanged rollback authority |
| candidate | `v1.231.0` / `27a8e54e7ac3c57d6cbf8891f0283dfbaee97317` | `997ffa7b641bfc0563a52e6190b548003316495d` | candidate context only |

Both commits were fetched by object ID from the official
`https://github.com/nautechsystems/nautilus_trader.git` repository into a private, task-owned bare cache. No moving branch or `latest` reference is an authority.

The portable contract binds 72 unique `commit:path -> blob` records with canonical SHA-256 `49b3f5ad6c369dfc888f56ed6f50dd88f32bca13848aa4f53d80dadba608ff6a`. A network-free verifier accepts only a bare cache explicitly supplied through `P1_U01_UPSTREAM_GIT_DIR`, resolves only the two exact commit IDs, and checks every binding. Before any object read it rejects all local `include.*`/`includeIf.*` directives, partial-clone/promisor configuration, and promisor pack markers. Every Git subprocess has a ten-second bound, disables lazy fetches, replacement objects, optional locks, hooks, all protocols, prompts, and inherited system/global Git configuration. If no cache is supplied it reports `DEFERRED` without a skip or xfail; a supplied missing, non-bare, included-config, incomplete, partial/promisor, or mismatched cache fails. This is deterministic inside the accepted cooperative-host boundary; it does not claim protection from a concurrent same-UID rewrite of the cache after inspection. The fixed 992-entry release manifest has canonical SHA-256 `2165c54983a1950e3054b0a164ff640a84e56c2ba187b8a0cf0de36dedbeda9a` and is also reconstructed from the verified candidate `RELEASES.md` blob `e3e774c9c3506e15036383a1994906e7775942fa`.

This packet consumes the accepted U00R inventory unchanged:

- path: `docs/implementation/p1-real-nautilus/upgrade/pin-inventory.json`
- SHA-256: `b9960c1153bfb89b10a0f3783d0afa5eb67af3fea2f805d8d37702b41bd4a0f9`
- Git blob: `fe91b838f0795784a5843cbdcf802326be644152`
- schema/tree: `nautilus-pin-inventory/v4` / `2e83bc2e811adf5d51a2a67a448d3efbadae3982`
- threat model: `U00R_TRUSTED_HOST_COOPERATIVE_GIT_V1`
- rollback pins: `PIN-705B6185D7861B184F80`, `PIN-426107DED6B955FE5AA6`, `PIN-FF46916D969407BE2268`
- candidate-context pin: `PIN-7D1CD22BD68E9DFEE446`

The source-contract test reads and hashes that inventory; it does not regenerate or normalize it. Root Python 3.11 parses local source and JSON only and never imports NautilusTrader. Candidate imports remain confined to a later isolated Python 3.12+ cell.

## Classification rules

Every bullet in the exact 1.228.0 through 1.231.0 release-note range is represented individually in the JSON manifest. Each entry records its immutable source line and text digest, release and section, classification, exact shared source binding, boundary, local disposition, optional highlighted release-delta record, and later scenario destinations:

- `DIRECT`: changes a used/planned Cython-v1 import, member, result, callback, cache, or accounting observation.
- `INDIRECT`: changes behavior that can reach a direct surface without changing its declaration.
- `V2_ONLY`: implemented in Rust/PyO3/v2 paths. It is never used as evidence for Cython-v1.
- `ADAPTER_ONLY`: belongs to a named venue adapter and is excluded from simulated-engine qualification.
- `BUILD_ONLY`: changes the isolated build/toolchain/dependency closure and is owned by U03, not runtime semantics.
- `NOT_RELEVANT`: neither imported nor planned for P1.

The shared closed dispositions (`RUST_PYO3_V2`, `VENUE_ADAPTER_OR_EXTERNAL_NETWORK`, `ISOLATED_BUILD_TOOLCHAIN`, and `P1_UNUSED_SURFACE`) compact repeated evidence without merging bullets. Named-adapter matching treats exact-case `Derive` as a venue name; lowercase verbs such as “derive price-based order IDs,” token-derived precision, and Rust serialization derives cannot become adapter work. The DeFi item is frozen as an unused pool-pipeline change, the L3 item is frozen at the L1-quote-and-bar/no-order-book-data P1 boundary, and serialization derives remain `V2_ONLY`. Deleting, adding, moving, or changing any bullet changes the fixed count, line/text membership, per-release/per-section counts, and aggregate digest, so the committed guard fails. P1-relevant direct, callback, reset, result, and accounting changes remain separate highlighted records.

## Release-delta ledger

### 1.228.0

| Classification | Exact upstream evidence | Local impact and destination |
| --- | --- | --- |
| `DIRECT` | candidate `nautilus_trader/cache/cache.pyx::Cache.reset`, blob `c2c3beff3662e7135a5de231f9b2fd2ac3f7a1bc` | Cache retains currencies, exchange-rate symbols, synthetics, and optionally instruments. `BEHAVIOR_REQUALIFY`: `P1-U06::cache-reset-retention`, `P1-U07::cache-state-parity`. |
| `DIRECT` | candidate `RELEASES.md:922`, deterministic backtest liquidation engine, release blob above | The fixed cash scenario must prove it acquires no unplanned liquidation behavior. `P1-U06::deterministic-liquidation`, `P1-U07::liquidation-parity`. |
| `DIRECT` | candidate `RELEASES.md::1.228.0/Fixed/duplicate AccountState`, blob `e3e774c9c3506e15036383a1994906e7775942fa` | Reset account-state cardinality can change. `P1-U06::reset-account-state-cardinality`, `P1-U07::reset-account-parity`. |
| `INDIRECT` | candidate `RELEASES.md::1.228.0/Fixed/calculated account state`, same release blob | Balance and commission observations can change. `P1-U06::calculated-account-balance`, `P1-U07::account-observation-parity`. |
| `DIRECT` | candidate `RELEASES.md:993`, multi-currency balance invariant, same release blob | The launcher reads `AccountBalance.total`, `.locked`, and `.free` and enforces `total == locked + free`. `P1-U06::multi-currency-balance-invariant`, `P1-U07::account-balance-field-parity`. |
| `DIRECT` | candidate `RELEASES.md:994`, NETTING reconciliation phantom reduce-only position fix, same release blob | The local engine observes NETTING position cardinality and state. `P1-U06::netting-no-phantom-reduce-only-position`, `P1-U07::netting-reconciliation-parity`. |
| `DIRECT` | candidate `RELEASES.md:1031`, `CurrencyPair.asset_class` crypto-pair correction, same release blob | Planned fixture-owned construction must probe the corrected field. `P1-U05::currency-pair-asset-class`, `P1-U07::currency-pair-parity`. |
| `INDIRECT` | candidate `RELEASES.md::1.228.0/Documentation/BacktestEngine shutdown notes`, same release blob | Release guidance records the pre-stop pending-fill caveat; it is not claimed as an API change. `P1-U05::pending-fill-callback-before-stop`, `P1-U07::callback-sequence-parity`. |
| `DIRECT` | candidate `RELEASES.md::1.228.0/Documentation/NautilusKernelConfig state flags`; candidate `nautilus_trader/system/config.py::NautilusKernelConfig.load_state/save_state` blob `a704b93b32f172dd1e02fcd69f379cb48333f54d` | The apparent default change is documentation-only. Executable inherited defaults are `False` at both endpoints. Both current local constructors omit these flags; U05 must explicitly set and probe both. `UNCHANGED_API`, `P1-U05::explicit-engine-config`. |
| `NOT_RELEVANT` | candidate `RELEASES.md::1.228.0/Python-v1 throttler`, same release blob | No current/planned P1 throttler use. |
| `BUILD_ONLY` | candidate `RELEASES.md::1.228.0/Internal Improvements/Rust 1.96.0 and Cython 3.2.5`, same release blob | Intermediate build history only; U03 owns the final candidate toolchain/closure and U05 owns its import probe. |
| `ADAPTER_ONLY` / `V2_ONLY` | candidate `RELEASES.md::1.228.0/Adapters` and `::Rust-PyO3-v2`, same release blob | Excluded; cannot justify any v1 change or external access. |

### 1.229.0

| Classification | Exact upstream evidence | Local impact and destination |
| --- | --- | --- |
| `DIRECT` | candidate `RELEASES.md::1.229.0/Fixed/NETTING realized PnL`, blob `e3e774c9c3506e15036383a1994906e7775942fa` | Realized PnL for reused `PositionId` and account-currency conversion changed. `P1-U06::netting-reused-position-id-accounting`, `P1-U07::netting-accounting-parity`. |
| `NOT_RELEVANT` | candidate `RELEASES.md::1.229.0/ProbabilityPriceFeeModel`, same release blob | Not imported/planned; local deterministic `FeeModel` subclass remains the contract. |
| `V2_ONLY` | candidate `RELEASES.md::1.229.0/Rust commission and portfolio`, same release blob | Rust commission-side/portfolio scoping does not replace U05/U06 custom-fee and accounting probes. |
| `ADAPTER_ONLY` | candidate `RELEASES.md::1.229.0/Adapters`, same release blob | Excluded from simulated Cython-v1 scope. |

### 1.230.0

| Classification | Exact upstream evidence | Local impact and destination |
| --- | --- | --- |
| `DIRECT` | candidate `RELEASES.md::1.230.0/Fixed/duplicate realized PnL`, blob `e3e774c9c3506e15036383a1994906e7775942fa` | Duplicate post-run realized PnL was corrected. `P1-U06::realized-pnl-cardinality`, `P1-U07::realized-pnl-parity`. |
| `NOT_RELEVANT` | candidate `RELEASES.md::1.230.0/event_store`, same release blob | No local Nautilus `event_store` import; `packages/engine_event_ledger` remains independent authority. |
| `BUILD_ONLY` | candidate `RELEASES.md::1.230.0/Build`, same release blob | Cython/toolchain work belongs to isolated U03 and the U05 import probe. |
| `ADAPTER_ONLY` | candidate `RELEASES.md::1.230.0/Adapters`, same release blob | Excluded and authorizes no venue access. |

### 1.231.0

| Classification | Exact upstream evidence | Local impact and destination |
| --- | --- | --- |
| `DIRECT` | candidate `nautilus_trader/backtest/engine.pyx::BacktestEngine.reset`, blob `285d1515eb261a61b6960ef8bc3235b8d90772b3` | Exchanges reset before execution/cache state; retained components/data/venues require explicit re-registration policy. `P1-U06::reset-order-and-reregistration`, `P1-U07::reset-state-parity`. |
| `DIRECT` | candidate `nautilus_trader/backtest/results.py::BacktestResult.summary`, blob `10028e9e822085b8c3712325b11e1218e7280001` | Additive `summary: dict[str, str]`. It may be recorded as diagnostics only, never accounting or execution authority. `P1-U05::result-shape-and-summary`, `P1-U07::summary-nonauthority`. |
| `DIRECT` | candidate `RELEASES.md:202`, simulated-venue multi-currency liquidation, FX rollover, and reset sequencing, release blob above; candidate `nautilus_trader/backtest/engine.pyx`, blob `285d1515eb261a61b6960ef8bc3235b8d90772b3` | Directly reaches the simulated Cython-v1 backtest path. `P1-U06::simulated-venue-multi-currency-reset-sequencing`, `P1-U07::simulated-venue-reset-fx-parity`. |
| `NOT_RELEVANT` | candidate `RELEASES.md:117`; rollback/candidate `crates/data/src/defi/engine.rs` blobs `a136a82e1f9f19459e9d8688d98fb656f93b56f5` / `3dc8de84fafb3f030011ae61a64c3cadc73f27a1`; `crates/model/src/defi/dex.rs` blobs `740b4701b3a919fb2d960b9ca647e2a536bc13c2` / `ed20e896724941fdb2601ceaf89ae087a91975f9` | Pool-to-`CurrencyPair` caching/publication is in the Rust DeFi pipeline. The fixed Cython-v1 single-instrument launcher imports neither that pipeline nor its pool publisher. This is not `ADAPTER_ONLY`, and it is not evidence for the separately planned Cython `CurrencyPair` fixture. |
| `NOT_RELEVANT` | candidate `RELEASES.md:139`; rollback/candidate `crates/model/src/orderbook/aggregation.rs` blobs `efeeaad4828c4752f82d1970dd9d2fb5cd78fc65` / `d1c9ca06d46fd65fa0ec704515c24dde4929c959` | `L3_MBO` preprocessing now hashes price when a `BookOrder` has a zero ID. The fixed P1 launcher supplies L1 QuoteTick and Bar only; no BookOrder/OrderBookDelta/L3 feed or surface; boundary `P1_L1_QUOTE_AND_BAR_NO_ORDER_BOOK_DATA`. The lowercase verb is not the Derive venue. |
| `INDIRECT` | candidate `RELEASES.md:147`, `:169`, and `:177`; rollback/candidate `crates/common/src/ffi/clock.rs` blobs `0b643b92680147ec47a7f029a684a221cf37572c` / `4e356d141d90e9c08a64985b4d4652bced1d5c44`; `crates/core/src/ffi/cvec.rs` blobs `216c0d8c4977545c3085d0b47e20a2ce1d48745a` / `1294263a21ad38d5abfd0a36c3fb61a1f20e3b0b`; rollback/candidate `nautilus_trader/backtest/engine.pyx` blobs `6b96344cf228869ed6ac46a407d5515c41c23061` / `285d1515eb261a61b6960ef8bc3235b8d90772b3`; `common/component.pyx` blobs `06f73791f0d264cf2e7c9727ce1321cd2c55528f` / `62b2eb69cf22a4c726dcf1df505936add2bcc56c`; `core/rust/common.pxd` blobs `634df8af5db7dc7400e5570cb42cc36f716addcd` / `6049816391e6df4138f4fc632115fe5ac30a81e2`; `core/rust/core.pxd` blobs `c86cc22412984267d9fab115d9f33abe527ddc70` / `9c361b9e853d700be32a16af5e4cb9c84af9c308` | Unsafe FFI access, Clone/Copy removal, and consuming reconstruction reach the Cython-v1 `BacktestEngine._advance_time` and `TestClock.advance_time_c` handler/drop path. Exact-cache assertions verify the safe/manual rollback reconstruction and candidate `unsafe`/`CVec::into_vec` implementation nodes. `P1-U05::cvec-clock-handler-ordering-and-drop`, `P1-U07::cvec-clock-handler-parity`. |
| `INDIRECT` | candidate `RELEASES.md:181`, out-of-order fill underflow/currency-mismatch fix, release blob above | Callback and accounting handling must remain deterministic. `P1-U05::out-of-order-fill-callback`, `P1-U06::out-of-order-fill-accounting`, `P1-U07::fill-order-parity`. |
| `DIRECT` | candidate `RELEASES.md:199`, cache venue-order-ID collision fix, release blob above; candidate `nautilus_trader/cache/cache.pyx`, blob `c2c3beff3662e7135a5de231f9b2fd2ac3f7a1bc` | Local proof reads cached order identity and filled state. `P1-U06::cache-venue-order-id-identity`, `P1-U07::cache-order-identity-parity`. |
| `INDIRECT` | candidate `RELEASES.md:362`, stale order-purge cache-index fix, release blob above | Cache order queries must not retain a stale index. `P1-U06::cache-order-index-after-purge`, `P1-U07::cache-order-index-parity`. |
| `DIRECT` | candidate `RELEASES.md:372`, backtest validation changes, release blob above | Replay order, error policy, duplicate venue, streaming book-state, funding, and FX rollover behavior require explicit probes. `P1-U05::backtest-replay-order-and-error-policy`, `P1-U06::duplicate-venue-and-fx-validation`, `P1-U07::backtest-validation-parity`. |
| `DIRECT` | candidate `RELEASES.md:374`, nondeterministic backtest event ordering fix, release blob above | Ordered callbacks and strategy events require exact later comparison. `P1-U05::deterministic-event-order`, `P1-U07::event-order-parity`. |
| `INDIRECT` | candidate `RELEASES.md:377`, reverse exchange-rate spread-side and route determinism, release blob above; candidate `nautilus_trader/cache/cache.pyx`, blob `c2c3beff3662e7135a5de231f9b2fd2ac3f7a1bc` | Can reach account-currency PnL observations. `P1-U06::reverse-xrate-side-and-route`, `P1-U07::xrate-accounting-parity`. |
| `INDIRECT` | candidate `RELEASES.md:378`, FX session daylight-saving boundary fix, release blob above | Candidate/rollback accounting must compare the same session boundary. `P1-U06::fx-session-dst-boundary`, `P1-U07::fx-session-boundary-parity`. |
| `INDIRECT` | candidate `RELEASES.md::1.231.0/Cython-v1 release posture`, blob `e3e774c9c3506e15036383a1994906e7775942fa` | Upstream calls 1.231.0 the intended final legacy Cython-v1 release, allows another 1.x only for a serious blocker, and moves `develop` to v2. This revalidates the fixed candidate; it does not move it. |
| `V2_ONLY` | candidate `crates/backtest/src/config.rs::SimulatedVenueConfig.validate` blob `3c8971f618188df7368cd7f8591c636568b4691c`; `crates/backtest/src/engine.rs::BacktestEngine.add_venue` blob `0156ea1c3702f9ffc5478e488d61a15c45485e6b`; `crates/execution/src/matching_engine/ids_generator.rs::IdsGenerator` blob `82c79303093df3bceada86d4a53ec2c36b26ce9f`; `crates/backtest/src/{exchange.rs,execution_client.rs}` blobs `d6afc6643bf0deb0204ee025e699b8effe18efd8`, `78f9e2dc2c0b0ae4bde7fc1ff8c61624ef12fc8b` | Venue registration/non-positive leverage, generated-ID collision, and simulated status-query fixes are implemented in Rust/v2 paths. Excluded from Cython-v1 evidence and acceptance. |
| `BUILD_ONLY` | rollback/candidate `pyproject.toml` blobs `ddf884887cf279f51cfc5e57a9571d4e562dff56` / `351fe55f9ff35d66ae0eca016bbd4654b56ef3f6`; rollback/candidate `Cargo.toml` blobs `ff51412d52194b2c051cd7b6d1a23d1ce5955fe4` / `ed4e5d0cefe12d50fd2270d6d6d07d2b8e5a39e5` | Python remains `>=3.12,<3.15`; Rust `1.95.0` to `1.97.1`; Cython `3.2.4` to `3.2.9`; setuptools `>=82` to `>=83`; fsspec becomes exactly bounded at `2026.2`; pandas `<3` to `<4`; pyarrow `>=23.0.1` to `>=25`; pytz `>=2026.1` to `>=2026.2`; tzdata `>=2026.3` added. U03 owns all build/closure decisions. |
| `ADAPTER_ONLY` | candidate `RELEASES.md::1.231.0/Adapters`, release blob above | Excluded; no broker, exchange, account, or network endpoint is in scope. |

## Direct API and invoked-member disposition

The JSON contract records endpoint commit, path, symbol, and blob for each surface. The compact table below is the human review index; it does not replace the JSON evidence.

| Surface group | Used/planned members | Impact | Local divergence and later test |
| --- | --- | --- | --- |
| `BacktestEngine` | constructor, `add_venue`, `add_instrument`, `add_strategy`, `add_data`, `run`, `get_result`, `reset`, `dispose`, `cache` | `BEHAVIOR_REQUALIFY` | Extract current qualification construction/run pattern; U05 lifecycle, U06 reset/re-registration, U07 parity. |
| `BacktestEngineConfig`, `LoggingConfig` | inherited endpoint defaults `load_state=False`, `save_state=False`; local arguments `run_analysis=False`, `bypass_logging=True` | `UNCHANGED_API` | Current constructors omit both state flags. U05 must explicitly set and probe them inside the isolated child; U01 does not change the launcher. |
| venue/account enums and objects | `Venue`, `AccountType.CASH`, `OmsType.NETTING`, `Money` | mixed | CASH scope stays fixed. NETTING and foreign-currency accounting require U06/U07 exact comparison. |
| instrument/currency | current `BTC`, `USDT`, `TestInstrumentProvider.btcusdt_binance`, returned `Instrument.id/quote_currency`; planned `Currency`, `CurrencyPair` | `ADAPT` / `BEHAVIOR_REQUALIFY` | Retire test-kit provider from product truth; construct from frozen local fixture metadata and probe both returned fields in U05, then compare in U07. `Instrument` endpoint blobs are recorded separately. |
| market data | `Bar` constructor and returned `.close`, `BarType.from_str`, `QuoteTick`, `Price.from_str/as_decimal`, `Quantity.from_str/as_decimal` | API unchanged; object validation behavior requalifies | Candidate FFI capsule changes do not alter the Cython-v1 Bar/QuoteTick constructor contract. Probe valid and boundary construction plus final-bar close in U05, compare normalized results in U07. |
| strategy/order callbacks | `Strategy` lifecycle and returned `rejected/semantic_events/entry_filled_quantity`, `cache.instrument`, `subscribe_bars`, inherited `OrderFactory.limit`, `submit_order`, `OrderFilled.last_qty/last_px`, `OrderRejected` callback | `BEHAVIOR_REQUALIFY` | `OrderFactory.limit` is bound at both endpoints to `nautilus_trader/common/factories.pyx` (rollback blob `b7c755bfe4a3ed2257f75200ff25a4ed2699ffab`, candidate blob `0020a2c7c1c6c5ffb326f76c8c462ad3fa83958d`). Strategy/callback declarations and every entry/exit invocation site are frozen; U05 traces callbacks and U07 compares sequence. |
| custom models | `FeeModel.get_commission`, `FillModel` constructor | fee behavior requalifies; fill API unchanged | Preserve deterministic local fee and fill configuration. U05 invokes the exact subclass callback; U06/U07 bind fee/accounting outputs. Upstream `PerContractFeeModel` spread-leg change is not used. |
| cache/accounting reads | `Cache.orders/positions/account_for_venue/reset`, `Account.balances/commissions`, returned `AccountBalance.total/locked/free`, `Position.quantity/avg_px_open/unrealized_pnl`, `Order.filled_qty` | `BEHAVIOR_REQUALIFY` | `AccountBalance` is bound to `nautilus_trader/model/objects.pyx` at both endpoints. These are observed engine evidence only; local result and event ledgers remain authoritative. U06/U07 cover cardinality, arithmetic invariants, values, reset state, and parity. |
| result/state | `BacktestResult.iterations/total_events/total_orders/total_positions/summary` | `ADAPT` | Wrap normalized authoritative fields; keep additive upstream `summary` diagnostic and non-authoritative. U05/U07. |

No direct imported class was removed. `OrderFilled`, `OrderRejected`, and `Strategy` source blobs are identical at the two endpoint commits:

- `nautilus_trader/model/events/order.pyx::{OrderFilled,OrderRejected}`: `d9643b00edc5049ea20894c5665398fdd273785e` at both endpoints.
- `nautilus_trader/trading/strategy.pyx::Strategy`: `07e30279524917221eb498040bf7907d05b248c8` at both endpoints.
- `nautilus_trader/backtest/models/fill.pyx::FillModel`: `66fc7adbfc73a8281854f8e55b86cb60e405f7a1` at both endpoints.

That byte identity resolves declaration ambiguity only. It does not waive later behavioral probes of callback sequence, cardinality, order state, commissions, balances, or PnL.

The 153-entry invocation map is the exact AST-discovered local ownership set for the three tracked Nautilus-importing files and has canonical SHA-256 `fca451e8a434486022e4b62e787aaf53cf9ad1d3ac85700df6c470521f68cf52`. It records path, qualified scope, source line, expression, access kind, member, and owning surface. The committed guard independently rediscovers imports (`ast.ImportFrom` and `ast.Import`) and scans every descendant scope for constructor, call, attribute, subclass, and bare runtime load of each lexically visible imported alias. Eighteen current bare values passed to `SimpleNamespace` or another call are explicit `ALIAS_ARGUMENT` records; annotations and already-recorded direct roots do not duplicate. Any other bare read, including an assignment RHS, becomes an invalid `UNCLASSIFIED_ALIAS_READ` sentinel and cannot enter the closed contract. Explicit root maps remain only for dependency-returned/dynamic objects and fields. Definitions, overrides, dynamic members, and returned-object fields are also resolved, and the complete set must equal the frozen map and digest. A committed in-memory pressure mutation uses `PriceAlias = Price; PriceAlias.from_str("1")` in the existing rejection callback and proves the assignment read is rejected even with unchanged imports and source-line count.

## Existing local path disposition

| Local path | Disposition | Boundary |
| --- | --- | --- |
| `engines/nautilus/launcher/nautilus_backtest.py` native import, venue, market-data, and simulation helpers | `EXTRACT` | Extract the already-qualified patterns into the future isolated runtime; keep the large launcher frozen as the rollback/source regression path. The legacy zero-order path is not product truth. |
| `engines/nautilus/launcher/target_portfolio_strategy.py` | `EXTRACT` | Extract strategy/callback patterns; do not expand this frozen qualification module. |
| `engines/nautilus/launcher/nautilus_paper_compat.py` | `RETIRE` | Compatibility diagnostic only; it cannot become the product runtime. |
| `packages/nautilus_backtest` | `REUSE` + `WRAP` | Reuse frozen fixtures/reference oracle and wrap normalized result validation; Nautilus observations never displace local authority. |
| `services/job_worker/nautilus_closure.py` and existing worker spawn/capture/fencing | `REUSE` + `WRAP` | Reuse custody and isolation; later add only the pinned P1 profile/result validator required by the master plan. |
| `packages/engine_event_ledger` and the job-store engine-event ledger | `REUSE` | Remain canonical local event/recovery authority. Nautilus `event_store` is not imported. |
| `services/paper_runtime` controller/custodian | `REUSE` + `WRAP` | Reuse the controller/custodian and later wrap a narrow isolated Nautilus session. No broker/exchange endpoint is introduced. |

## Executable traceability contract

- **P1-U05** must prove exact candidate/version import identity in the isolated cell, explicitly set and probe `load_state=False` and `save_state=False`, exercise every direct constructor/member, deterministic venue/instrument/market-data construction, custom fee/fill callbacks, strategy lifecycle, callback/event ordering, result shape, and diagnostic-only summary handling.
- **P1-U06** must prove CASH/NETTING balances, commissions, order and position cardinality/state, foreign-currency and reused-`PositionId` realized PnL, duplicate-PnL absence, and reset/cache/re-registration behavior.
- **P1-U07** must run the same frozen fixtures against the exact rollback and candidate cells and compare normalized strategy events, callback order, order/position/accounting outputs, reset state, and authoritative result fields. Candidate-only `summary` cannot make parity pass or fail except for its explicit non-authority assertion.

If any later probe exposes a new direct/callback/accounting behavior not represented by this closed contract, qualification stops as `BLOCK`; it is not dynamically adapted, skipped, xfailed, or inferred from a v2 implementation.

## Non-authorizations

This source-only delta does not build, install, activate, promote, or productize 1.231.0. It does not change the rollback candidate, runtime closure, engine policy, database, services, paper controller, live gates, or production state. It authorizes no Qlib, TimesFM, agent, PageIndex, memory, Semantica, broker/exchange, network, account, or trading access.
