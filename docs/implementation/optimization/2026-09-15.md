# Optimization implementation — 2026-09-15

Historical campaign checkpoint. Current M8/M9 source wiring and independent-review
repairs are recorded in [bounded session source](session-source-2026-09-16.md) and
[review dossier](m8-m9-independent-review-2026-09-16.md). The unfinished-source
statements below describe their recorded checkpoint, not the current candidate.

## Scope and source identity

Candidate branch: `codex/optimization-20260915`, based on
`8460e129a8f0d18f4a93f01e1e1ecd82a1e46552`. Exact commit/tree and validation
results are recorded in the external `CANDIDATE.json` evidence ledger.
The canonical checkout, its user changes, and the P3 S/T checkpoints are preserved.
S/T calculations and authority consumers are integrated selectively; this is not
a claim that their historical qualification applies to this candidate.

## Implemented source changes

- Developer feedback builds native custody on demand in private test storage,
  validates the caller's complete extension identity, reports both static graphs,
  and cleans up the owned test group on normal exit and termination.
- Python error-level checking is part of Foundation CI. Core's 117 reported
  errors were repaired without increasing baselines or disabling rules.
  Shared contract models and publication/status types remove import cycles.
- Baseline pruning is an intersection of diagnostic multisets. Core decreased
  from 5,508 to 5,352; legacy remains 13,241. These are retained typing debts,
  not runtime bug counts. Zero error-level output does not mean fully typed code.
  Core warning-level diagnostics outside the baseline rose from 3,638 to 4,559
  after S/T integration, then decreased to 3,795 through boundary annotations
  and validator reuse (764 fewer this continuation; still 157 above the parent).
  The subsequent M8 source adds 10 outside-baseline warnings (3,805 total,
  167 above the original parent), from reuse of existing private helpers.
  The subsequent profile/catalog/native-request integration adds four private-helper
  usage warnings (3,809 total), with no additional Any/Unknown diagnostics.
  Pair/exit reconstruction then leaves 3,812 warnings: five additional uses of
  existing private helpers and two fewer net Unknown diagnostics; Any is unchanged.
  The retained core baseline stays 5,352; pruning found no further obsolete
  entries. No rules were relaxed, suppressions added or new baseline debt admitted.
- Six copies of the JSON-array validator now use one shared implementation.
  Worker spawn carries its already-validated campaign payload through consumption;
  output custody, readback, fixture and calculation boundaries use existing types.
- Holdout replicas require a fresh pre-spawn callback; rejection closes transport
  descriptors without starting a child. The validated view's public bytes are
  read-only. This hook does not authenticate a custodian or open official HOLDOUT.
- Parity rejects invalid quote quantum, noncanonical traces and mismatched replica
  receipt bindings. ExitResult requires all 13 distinct checks and a consistent
  PASS/FAIL verdict. These guards do not implement native execution or recompute
  the complete phase-exit decision.
- Receipt publication validates before creating output, completes short writes,
  removes only its own failed output, and distinguishes valid stale qualification
  from malformed authority. Receipt-only descendants retain semantic identity;
  intervening source or closure-policy changes invalidate qualification.
- Shared P3 approval/token readers open with `O_NONBLOCK` before checking file
  type. A FIFO without a writer now rejects promptly instead of hanging preflight;
  two real-subprocess regressions reproduce the former hangs.
- Seven identical worker blocked-claim branches share finalization. Three legacy
  report loaders share file selection while preserving individual parsing behavior.
  Parity reuses the typed canonical artifact reader instead of its duplicate
  protocol/parser; noncanonical reference, native result and receipt now reject.
- Removed 22 unused dashboard/debug files (1,736 lines) and seven unreferenced
  private Python helpers (134 lines). The Git descriptor helper is retained because
  its bytes belong to a frozen authority pin. Hook and public API candidates remain.
  Source-string-only assertions were replaced or narrowed to actual consumers.
- P3 daily acquisition receipts feed existing P2 materialization and revision
  contracts; no fourth artifact store or provider graph was introduced.
- Selected S/T fixes cover first-valid TSMOM observations, fixed Decimal rounding,
  final-signal suppression, capacity dates, H1-only results and open-only buffer
  data. Holdout child input is an exact 681-artifact immutable view, transported
  through one sealed descriptor; its file-descriptor limit remains 256.
- P3 post-run retention/recomputation checks heartbeat and deadline. Protected
  profiles bind source, payload and job; disclosure requires committed SQL readback
  and does not repeat uncertain writes. No production database was changed.
- Hosted CI delegates backend/dashboard execution to governance topology once.
  Local `test-all` keeps its component checks. The closure validator still requires
  the exact workflow, including a retained output before promotion attestation.
- Python pin extraction caches the immutable line-offset table for one source
  text. Three extractions of the same runtime-closure module decreased from
  31.65 to 15.02 seconds on this host, with identical result digests; this is a
  local microbenchmark, not an overall CI timing claim. All 248 parser tests pass.
- The unused helper in the pinned sealed-executor materializer is retained: its
  deletion changes the authority-bound source digest and needs requalification.

## Approved M8/M9 continuation

The operator approved [the private source/test design](holdout-native-seams.md)
with “Duyệt”. This approval does not provision a custodian, run a migration or
authorize an official experiment.

M8 now has canonical attestation/release models, a domain-separated commitment,
kernel-authenticated Unix socket delivery and exactly one sealed read-only memfd.
The research parent validates metadata before committed SQL disclosure; the
custodian independently claims and reads back its one-use release before loading
plaintext. It rechecks its profile and live claim before sending. The received
365 days plus buffer become the existing immutable 681-artifact calculation view;
raw plaintext is not copied into the research CAS. Truncated/extra descriptors,
incorrect ownership/seals/digests/inventory and uncertain release attempts fail
closed and received descriptors are closed.

Migration `0027_p3_custodian_release` and its concrete SQL client define a
separate restricted custodian capability, append-only
release identity, fresh role/claim checks and committed readback with no retry.
The opt-in disposable-cluster fixture now exercises the concrete custodian client
through real psycopg connections, transactions and committed readback. A fixture-only
settings adapter selects the private Unix socket; production settings remain TCP-only.
The migration chain through 0027, competing one-use releases, stale bindings,
expiry and forbidden table access pass. Ten catalog drift vectors additionally
cover table/column/function privileges, trigger disablement, function search path,
security mode/strictness, role membership/settings and rewrite rules. Every query
checks the pinned catalog; restoring the schema permits readback again. The
cluster runs without TCP and is stopped and removed. Four existing psycopg_pool
deprecation warnings remain; no production database was touched.

The catalog snapshot query is shared with migration 0026 without changing that
migration's snapshot bytes or historical expected hashes. The custodian snapshot
also binds SQL helper definitions, privileges, roles/settings and release history
schema; it does not contain row data, credentials, database names or OIDs.
Protected custodian profiles bind the exact request, current source/gates, endpoint,
metadata root, credential directory and catalog digest. The profiled connection
checks its UID before reading credentials and rechecks the profile immediately
before loading plaintext as well as before sending. Official HOLDOUT profiles
require a custodian endpoint. Default fixtures and the official database profile
remain at 0026; official-worker release admission and real cross-UID qualification
are still incomplete.

M9 now has an actual Nautilus 1.231.0 next-open calculation adapter, tested with
the pinned wheel from the retained local cache. At boundary−1 ns an internal
zero-size quote removes executable liquidity. The engine emits OrderSubmitted
at the boundary; its fixed 1 ns latency queue delivers the order for execution
with the boundary+1 ns quote. Tests verify submission-before-quote and exact fill
timestamps, not venue acceptance before the quote. Bars clock precomputed targets
only; bar execution stays disabled. Orders, fills, fees, available cash and
positions come from the engine; the synthetic reference is an independent oracle.

Four scenarios, each repeated in three fresh native processes, cover round trip,
loss/re-entry, unchanged long position and no trades. Malformed input, duplicate
keys, invalid day/boundary pairs and missing terminal liquidation reject.
This proves synthetic adapter behavior; it is not a protected native launch,
role-bound six-run parity envelope, runtime closure or official receipt.

M9's private request producer now derives exactly 366 next-open steps from the
immutable released view for PRIMARY or SELECTED_BASELINE. Requests bind source,
environment, manifest, instrument, policy, transitions and row references; request
bytes remain in memory and are not written into CAS. Canonical validation recomputes
the expected request and rejects role/source/environment substitutions. Reference
and native paths share the frozen target producer and result normalization; their
accounting implementations remain separate. A full synthetic year passed in three
actual pinned engine processes per role, including trace identities, exact fields,
fee tolerance, ending cash and flat terminal position. This is diagnostic execution;
the protected P1 launcher and parent-observed parity envelope remain to be connected.

The private parity-pair builder now recomputes both retained public comparisons,
requires six distinct role-bound receipts with the expected source, environment,
sandbox policy and trial, and verifies executable summary arithmetic from retained
traces. A digest-valid forged return/drawdown cannot pass the pair validator.
The public ParityResult contract remains unchanged. Launch provenance must still
come from parent-observed protected execution; synthetic receipts do not supply it.

`evaluate_phase_exit` reconstructs selection, qualification/head bindings, holdout
calculations, replay, both reference paths and the native pair before calculating
all 13 exit checks. It uses the frozen Decimal boundaries. Invalid provenance
raises; economic failure preserves FAIL; parity mismatch produces HELD.
`prepare_phase_exit` reuses the existing single-primary EXIT_DECISION proposal,
registry transition and deterministic publication builder. HELD cannot publish.
The regression uses declared selection/qualification/native-receipt unit inputs;
its holdout arithmetic, replay validation, comparisons and failed exit decision
are actual computations, not an official experiment or SQL qualification.

The evaluator presently requires the already released in-memory calculation
view together with retained research/output artifacts. It must not be connected
to a later independent job by reopening holdout or writing plaintext into CAS.
The worker still needs to own that lifetime and the fresh pre-replica SQL fence;
official HOLDOUT, PARITY and PHASE_EXIT admission remains closed.

## Runtime paths

The V2 worker already uses `RuntimePathsV2` from its attested runtime authority.
Regression checks now exercise two different deployment roots while deliberately
pointing the compatibility constants elsewhere. Environment overrides, unsafe
owners/modes and symlinks remain rejected.

The remaining 16 files containing the former account path are not one portable
configuration surface:

| Group | Treatment |
| --- | --- |
| Worker V2 environment | Uses attested paths; compatibility constants retained |
| Phase-1 safety, canonical safety source, Control API approval, semantic refresher | Fixed authority contract; requires reviewed runtime migration |
| `runtime_release/paper_application` and `paper_backend` | Preserved release projections; do not edit as generic shared helpers |
| Native toolchain/materialization scripts and topology/audit tools | Explicit host policy; no ambient `HOME` or environment fallback |

This change does not claim that all legacy runtime paths are portable. Changing
a source allowlist does not migrate protected files or install a new authority.

## Data preparation connection

`acquire_p3_binance_daily.py --receipt-jsonl` emits retained acquisition references.
Its default human-readable output remains available. Provider execution still
requires its own operator authorization.

For already retained acquisitions, `seal_p3_dataset.py INPUT STORE
--acquisition-refs` accepts an object with `query` and `acquisition_refs`. References
must be unique and ordered by day/fetch time, and cover exactly the 2,800 research
days. The query cutoff must include actual materialization time. It returns
`dataset` and `revision_inventory_ref`; the existing backup/commitment/review
producers then consume those artifacts. Pre-materialized P2 input remains supported.
The 2,800-day integration test uses synthetic archives and independently verifies
the resulting revision inventory. Subsequent authorized public-archive acquisition
validated 38 research days, from 2018-01-01 through 2018-02-07. The official
2018-02-08 daily archive has a matching checksum but closes at 00:28:14.788 UTC;
the frozen complete-day policy correctly rejects it. Original bytes and rejection
evidence are retained outside Git. No missing day was filled or silently skipped,
and no holdout data was accessed. The real 2,800-day dataset remains incomplete.

## Remaining boundaries and work

This is not a completed M0–M12 qualification:

1. Complete protected holdout admission → independent custody → durable consume
   → committed readback → released view → fresh spawn fence. The view object and
   SQL disclosure client alone grant no release authority; normal HOLDOUT stays
   closed in the official worker and SQL lane.
2. Connect the private native request/adapter to the protected P1 launcher,
   retain both roles' parent-observed parity envelope, connect the implemented
   phase-exit evaluator and EXIT_DECISION proposal to worker custody and SQL. Do not
   replace the native execution proof with the synthetic reference calculator.
   No phase-exit receipt is manufactured by this optimization.
3. Disposable migration-chain testing through 0027 has passed under separate
   approval, including the concrete custodian client and catalog drift rejection.
   Complete protected deployment/TCP and cross-UID qualification; this synthetic
   SQL test grants no production migration authority.
4. Complete clean-candidate aggregate and genuine hosted Foundation qualification.
   The local aggregate passed clean-source lineage and recorded 8,416 passed,
   six failed, 251 skipped and 46 deselected before a diagnostic interruption.
   Its stack was in artifact writing; this does not prove a hang. The six failures
   exposed stale generated status, a deleted pinned helper, the P3 adapter being
   inside the frozen P1 inventory, and missing P3 direct-API coverage. Repairs
   preserve P1 runtime pins and grant no qualification through source assertions.
5. Complete the approved M8/M9 private contracts' remaining connections; no new
   source-design approval is required for that scope. Legacy remains supported;
   no retirement was assumed.

Keep source verification, immutable SQL evidence, hosted qualification,
installation, protected runtime, and live authority as separate verdicts.

## Worktree archive and actual space accounting

The private evidence directory is
`/home/thenam176/projects/trading-agent-optimization-evidence/2026-09-15`.
`worktree-archive/MANIFEST.json` names the exact 79 old checkouts. Backups include
all working files, a full-history Git bundle, original index metadata, binary
patches and 2,778 indexed objects. All 79 restored indexes and staged/unstaged
patches match. GNU tar content/mode comparisons passed for both the extracted
copy and the originals. The complete 2,521,788-path inventory also matches,
including detection of newly added files. SHA256SUMS passed. The first extraction masked mode bits;
those bits were restored before the successful comparison.

After explicit operator approval, all 79 selected originals were removed.
`worktree-archive/REMOVAL-RESULT.json` records PASS: selected paths absent,
Git refs unchanged, canonical user changes and S/T preserved. The measured
filesystem free-space increase was 41,452,466,176 bytes (41.45 GB). The private
archive remains; unrelated checkouts and stale registration metadata were retained.

| Space measure | Bytes | Decimal GB |
| --- | ---: | ---: |
| Unique allocation under the 79 selected paths | 52,887,838,720 | 52.89 |
| Still hardlinked outside that selection | 11,453,579,264 | 11.45 |
| Estimated allocation reclaimable by deleting originals | 41,434,259,456 | 41.43 |
| Retained archive files, top level | 15,707,783,168 | 15.71 |
| Estimated net benefit after retaining archives | 25,726,476,288 | 25.73 |

The table gives inode/block estimates; the measured deletion delta is above. Restore steps
are in `worktree-archive/RESTORE.md`. The archive is private and must not be
uploaded or committed; it contains ignored working-tree material.

## Verification ledger

Foundation's verify job has a bounded 120-minute budget. Run `35072598863`
on `67942d8` exceeded the former 90-minute limit during critical coverage,
after the governance report recorded 11,419 passed and zero failed. The earlier
successful run `35063601089` spent 26 minutes 40 seconds in coverage; the later
run reached that step with only about 22 minutes left. Its portable root suite
took 59 minutes 26 seconds, versus 46 minutes 38 seconds previously. The new
budget leaves room for that measured variation without removing tests, changing
coverage floors or changing the separate host-authority workflow. The structural
validator requires exactly 120 minutes; successful hosted completion must still
be observed on the new commit.

Results apply only to the exercised source scope. Root source verification was
completed in parts, with fresh reruns of observed source failures. This is not
a single clean-candidate aggregate or hosted qualification PASS.

| Check | Result |
| --- | --- |
| Pair/exit, spawn, output validation, lifecycle and publication recovery | 119 passed; synthetic owner inputs are explicitly scoped in exit tests |
| Actual pinned native adapter including per-row equity comparison | 9 passed; diagnostic execution, not protected launch qualification |
| Pair/exit core type graph | 0 errors, 3,812 warnings outside baseline; 345 files; retained baselines unchanged |
| Latest M8/M9 profile, custody, reference, view and parity regression | 174 passed, 2 host-native cases deselected |
| Latest native diagnostics including both complete 366-step roles | 9 passed; 18 actual engine scenario processes across overlapping tests |
| Latest core type graph | 0 errors, 3,809 warnings outside baseline; 344 files; 4 new private-helper reuse warnings, no new Any/Unknown diagnostics |
| Latest Ruff / audit / secret hygiene / maintainability | PASS |
| Latest disposable SQL through 0027 with concrete client and 10 catalog drift cases | 1 passed in 92.47 seconds; 4 pool warnings; cluster stopped and removed |
| Latest P3 portable regression (`not runtime_postgres and not host_coupled`) | 1,099 passed, 7 deselected in 1,213.86 seconds |
| Approved M8 release/disclosure/view/profile/fixture regression | 66 passed, 1 opt-in SQL skip; final SQL-client rerun 6 passed |
| Separately approved disposable SQL migration chain through 0027 | 1 passed in 91.13 seconds; 4 pool deprecation warnings; cluster stopped and removed |
| M9 pinned native adapter, UTC and event/init timing | 5 passed; four scenarios each repeated in three fresh native processes |
| Prior M8 core basedpyright 1.39.10 | 0 errors, 3,805 warnings outside baseline; 341 files analyzed |
| Latest native source boundaries | PASS; clean committed lineage subsequently passed |
| `scripts/dev.py static` (Ruff and both pinned error-level type graphs) | PASS; 0 error-level diagnostics after retained baselines |
| `make audit` on this dirty candidate | PASS |
| Continuation core basedpyright 1.39.10 | 0 errors, 3,795 warnings outside baseline; 337 files analyzed |
| Continuation contracts/parity/view/replicas/readback/spawn/startup | 236 passed |
| Continuation output custody/output validation/integration/startup | 73 passed |
| Continuation disclosure/profile/composition/input/database-profile unit checks | 60 passed; no SQL qualification |
| Retained-family selection and contract regression | 17 passed |
| Root continuation using canonical source-only marker selection | 6,667 passed, 238 skipped, 24 deselected; 73 cached-validator failures corrected and all 73 passed on exact-node rerun |
| Contract generator `--check` | PASS |
| Dashboard tests, typecheck, lint, production build | PASS; 199 tests plus isolated security integration |
| Worker behavior before/after deduplication | 158 passed, 23 unchanged SQL-profile skips |
| Legacy report loader behavior / full backend suite | 6 focused; full suite 507 passed, 2 skipped |
| Deleted Python helper scope | 1,407 passed |
| P3 protected profile/startup/readback/composition | 269 passed |
| P3 calculation-view/reference/storage | 62 passed |
| Synthetic 2,800-day acquisition-to-P2 CLI bridge | PASS |
| Bootstrap/native identity/termination, including TERM-resistant descendant | 11 passed |
| Pin extractor after bounded cache | 248 passed |
| Canonical dashboard introduction/current-file custody | 4 passed |
| CI repository shape and D0 | 15 passed and 11 passed |
| Closure validator and nested authority-boundary checks | 155 passed |
| Parity canonical reader and executable reference | 7 passed |
| `make check-p0-maintainability` | PASS; no growth budget increased |
| Clean local `make test-all` | Lineage PASS; interrupted after 8,416 passed and six integration failures; see current evidence ledger |
| Hosted `ci-portable` qualification | Pending; exact candidate/run state is recorded in the external ledger |
| Aggregate repair | 134 focused, 21 parity/exit and 9 native tests passed; legacy 507 passed, 2 skipped; static and hygiene PASS |
| Independent Codex Security review | NOT REVIEWED; tool unavailable |
| Runtime SQL, protected holdout, installation, broker/live execution | NOT RUN |

An initial broad command accidentally selected the opt-in runtime tests; both
failed at missing-approval validation before runtime actions. That run recorded
3,088 passed, 24 skipped and six failures before being interrupted. Three failures
were obsolete CI/dashboard assertions, one was the pinned materializer deletion
(now restored), and two were the unselected-runtime authority requirements.
Remaining source checks used the Makefile's `not runtime_postgres and not
host_coupled` selector. They recorded 6,667 passed, 238 skipped and 24 deselected.
Their 73 failures were restricted to two modules that had loaded the old workflow
validator before its repair. A fresh 155-test run of those modules passed, then
an exact-node rerun of all 73 reported failures also passed on final source.
No approval or source gate was bypassed to turn a HOLD into PASS.

The first hosted PR run exposed an exit-policy mismatch in basedpyright 1.39.10:
GitHub/JSON formatting counts warnings even with `--level error`, while terminal
formatting filters them before counting. Both project configurations now set
`failOnWarnings: false` explicitly for the approved error-level gate. Diagnostic
severity, baseline files and error rejection are unchanged. The offline CLI
regression exercises warning-only and type-error inputs using each actual config.

## Candidate handoff and limits

The source snapshot and binary patch are recorded in `CANDIDATE.json` and
`candidate.patch` in the private evidence directory. The candidate now has local
checkpoint commits; CANDIDATE.json identifies the exact current HEAD/tree and
remote state. The canonical checkout and preserved S/T changes remain untouched.
Source commits and generated status are not qualification receipts.

Cleanup removes the candidate's task-created Python environments, dashboard
Node dependencies/build output and bytecode caches after verification. Recreate
the owning component's environment using the frozen setup commands in README
before another validation run. Canonical-checkout environments are untouched.

The project as a whole is **not yet optimized to the full M0–M12 end state**.
S/T integration adds source and tests, so net lines increase despite proven dead
code removal. Warning-level typing debt remains above the parent as recorded above.
M8 still needs the official-worker connection and cross-UID proof; its concrete
profile and disposable SQL checks exist. M9 has a request producer, both-role
comparison, exit calculator and publication proposal; it still needs protected
native launch, one released-view lifetime and official-worker/SQL wiring. Those are
unfinished source tasks, not merely missing external run permissions. Keep the
official lanes closed until their implementations and independent checks exist.
