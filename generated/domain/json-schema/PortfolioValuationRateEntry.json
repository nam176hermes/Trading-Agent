{
  "$defs": {
    "Currency": {
      "description": "One immutable identity issued by a versioned currency registry.",
      "enum": [
        "USD",
        "USDT",
        "BTC",
        "ETH"
      ],
      "title": "Currency",
      "type": "string",
      "x-currency-registry-version": "currency-v1"
    }
  },
  "additionalProperties": false,
  "properties": {
    "account_id": {
      "maxLength": 64,
      "minLength": 1,
      "pattern": "^[A-Za-z0-9][A-Za-z0-9._:-]{0,63}$",
      "title": "Account Id",
      "type": "string"
    },
    "effective_at": {
      "format": "date-time",
      "title": "Effective At",
      "type": "string"
    },
    "provenance_id": {
      "maxLength": 64,
      "minLength": 1,
      "pattern": "^[A-Za-z0-9][A-Za-z0-9._:-]{0,63}$",
      "title": "Provenance Id",
      "type": "string"
    },
    "quoted_at": {
      "format": "date-time",
      "title": "Quoted At",
      "type": "string"
    },
    "rate": {
      "pattern": "^(?:0|-?[1-9]\\d*|-?(?:0|[1-9]\\d*)\\.\\d*[1-9])$",
      "title": "Rate",
      "type": "string",
      "x-canonical-decimal-policy": "decimal-v1"
    },
    "schema_version": {
      "minLength": 1,
      "title": "Schema Version",
      "type": "string"
    },
    "source_currency": {
      "$ref": "#/$defs/Currency"
    },
    "target_currency": {
      "$ref": "#/$defs/Currency"
    }
  },
  "required": [
    "account_id",
    "effective_at",
    "schema_version",
    "source_currency",
    "target_currency",
    "rate",
    "quoted_at",
    "provenance_id"
  ],
  "title": "PortfolioValuationRateEntry",
  "type": "object"
}
